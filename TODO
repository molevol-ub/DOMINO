## TODOs (for developers only)

- Wrap the package into docker

- install.sh
	Check Installation somehow
	
- Check every time you run any DOMINO script a small script that checks every module/dependency is OK: 
	Update: DM_dependencies.pl
	
